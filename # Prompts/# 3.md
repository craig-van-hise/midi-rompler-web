# Product Requirements Prompt (PRP) — 🔵 Active

## 1. Project Context & Objectives
The previous attempt to synthesize the Electric Piano has been abandoned. The objective is to restore the `Tone.Sampler` architecture for all instruments and configure a local asset hosting pipeline specifically for the "Electric Piano" patch. The user will provide the actual high-fidelity sample files (e.g., Rhodes `.wav` or `.mp3` files). The worker agent must configure the routing and directory structure so the application correctly fetches these local assets when the Electric Piano is selected.

## 2. Technical Decisions & Dependencies
* **Asset Hosting:** The `electric-piano` will be served locally from the Vite `/public` directory.
* **Base URL Routing:** The `AudioEngine` must dynamically switch the `baseUrl`. If the instrument is `electric-piano`, it must point to the local `/samples/electric-piano/` directory. All other instruments remain on the `nbrosowsky` CDN.
* **Sample Map:** We will define a standard multi-sample map for the electric piano using C octaves. 

## 3. Implementation Phases

### Phase 1: Directory Setup & Engine Routing — ✅ Completed
* **Objective:** Create the local sample directory and refactor `engine.ts` to route the `electric-piano` to local assets.
* **Instructions:**
  1. Ensure a directory exists at `public/samples/electric-piano/`. (Create it if it does not exist).
  2. Open `src/audio/engine.ts`.
  3. Ensure the `sampler` class property is strictly typed back to `Tone.Sampler | null`.
  4. In `getSampleMap(instrument: string)`, update the `electric-piano` block to expect a standard set of octaves. Example:
     ```javascript
     if (instrument === 'electric-piano') {
         return {
             'C2': 'C2.mp3',
             'C3': 'C3.mp3',
             'C4': 'C4.mp3',
             'C5': 'C5.mp3'
         };
     }
     ```
     *(Note: If the user provides `.wav` files, update the extensions in the map above to `.wav`)*
  5. In `loadInstrument(instrument: string)`, implement dynamic `baseUrl` logic:
     * If `instrument === 'electric-piano'`, set `baseUrl = '/samples/electric-piano/'`.
     * Else, set `baseUrl = \`\${BASE_URL}\${folderName}/\``.
  6. Pass this computed `baseUrl` into the `Tone.Sampler` constructor.
* **TDD Checkpoint:**
    * **Test Case 1:** [Given] `instrument` is `'electric-piano'`, [When] `loadInstrument` is called, [Then] assert the `Tone.Sampler` constructor receives `baseUrl: '/samples/electric-piano/'`.
    * **Test Case 2:** [Given] `instrument` is `'piano'`, [When] `loadInstrument` is called, [Then] assert the `Tone.Sampler` constructor receives the external CDN `baseUrl`.
    * **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Write and execute these tests. Once they PASS, set status to ✅ Completed. Do not proceed until status is ✅.

### Phase 2: Asset Placement & System Validation — 🔵 Active
* **Objective:** Pause automation to allow the human developer to inject the actual audio assets into the newly created local directory.
* **Instructions:**
  1. Verify the `public/samples/electric-piano/` directory exists.
* **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. 
* **HUMAN VALIDATION REQUIRED:** STOP. The agent cannot download authentic Rhodes samples. Ask the user to:
  1. Acquire 4 electric piano samples (C2, C3, C4, C5).
  2. Format them as `.mp3` or `.wav` (and ensure the map in Phase 1 matches the extension).
  3. Place them exactly in the `public/samples/electric-piano/` directory, naming them `C2.mp3`, `C3.mp3`, etc.
  4. Start the dev server (`npm run dev`), select "ELECTRIC PIANO", and play a chord to verify local loading works without 404 errors.
  * Wait for the user to confirm the files are placed and the sound is working. Once confirmed, set this Phase's status to ✅ Completed.

## 4. Final Review & Validation
* **Technical Audit:** Verify that `engine.ts` cleanly separates the local asset logic from the CDN logic without redundant conditional fallbacks.
* **Status Reconciliation:** Verify all sub-phases above are marked ✅ Completed. If any are 🚨 Error or 🔘 Pending, you may not proceed.
* **Final Human Sign-off:** Ask the user to confirm the Rompler correctly loads and pitches the new local samples, and confirm the top-level PRP status can be safely marked as ✅ Completed.