# Product Requirements Prompt (PRP) — 🔘 Pending

## 1. Project Context & Objectives
The application is throwing a Vite/ESBuild compilation error (`"await" can only be used inside an "async" function`) on line 160 of `engine.ts`. This occurred because asynchronous operations (`await fetch`) were added to the `loadInstrument` method, but the method signature was never updated to reflect its new async nature. The objective is to fix this syntax error immediately.

## 2. Technical Decisions & Dependencies
* **TypeScript Syntax:** Add the `async` keyword to the `loadInstrument` method signature.
* **Return Type:** By making it `async`, it implicitly returns a `Promise<void>`. The React UI (`App.tsx`) is already equipped to handle asynchronous instrument loading, so no UI changes should be strictly required for compilation.

## 3. Implementation Phases

### Phase 1: Async Signature Patch — ✅ Completed
* **Objective:** Fix the syntax error in `engine.ts`.
* **Instructions:**
    1. Open `src/audio/engine.ts`.
    2. Locate the `loadInstrument(instrument: string)` method declaration.
    3. Add the `async` keyword to the signature:
       ```typescript
       async loadInstrument(instrument: string) {
           // ... existing logic
       }
       ```
    4. Save the file and verify the Vite HMR (Hot Module Replacement) successfully rebuilds without throwing the 500 Internal Server Error.
* **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. Apply the keyword. Once compilation passes, set status to ✅ Completed.

### Phase 2: System Polyphony & Sustain Validation — 🔵 Active
* **Objective:** Resume the validation step that was interrupted by the crash.
* **Instructions:**
    1. Ensure the dev server is running.
* **AGENT INSTRUCTION:** Set Phase status to 🔵 Active. 
* **HUMAN VALIDATION REQUIRED:** STOP. Ask the user to:
    1. Open `localhost:3000`.
    2. Select "Strings" from the UI.
    3. Verify the loading spinner clears successfully.
    4. Hold a chord down for 5+ seconds. Verify the strings sustain indefinitely, pulled natively from the base64 payload.
    * Wait for the user to confirm success. Once confirmed, set this Phase's status to ✅ Completed.

## 4. Final Review & Validation
* **Technical Audit:** Verify the Vite dev server console is completely free of syntax errors.
* **Status Reconciliation:** Verify all sub-phases above are marked ✅ Completed.
* **Final Human Sign-off:** Ask the user to confirm the top-level PRP status can be safely marked as ✅ Completed.